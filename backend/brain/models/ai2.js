class AI2 {
    async ask(prompt) {
        // Replace this stub with the real integration for your second AI source.
        const response = `AI2 response to: ${prompt}`;
        return response;
    }
}

export default new AI2();
