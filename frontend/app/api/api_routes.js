// frontend/app/api/api_routes.js

/**
 * APIRoutes
 *
 * Centralized route definitions for the frontend.
 * Keeps all API endpoint paths in one place.
 */

const APIRoutes = {
  RUN: "/api/run",
  STATUS: "/api/status",
  HEALTH: "/api/health"
};

export default APIRoutes;
